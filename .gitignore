# ── Generated by build.py ───────────────────────────────────────────────
# The packaged copy of the web app (index.html with its What's New tab stripped
# and CDN URLs rewritten) plus the vendored front-end libraries. Rebuild with
# `python build.py`.
# web/

# Packaged plugin zips, rebuilt on every run.
# dist/

# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/
ROADMAP.md
resources/make_icon.py

# Virtual environments
.venv/
venv/
env/

# ── Editors and IDEs ────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
*.bak
*.orig
*.rej

# ── OS clutter ──────────────────────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# ── Local test output ───────────────────────────────────────────────────
# DEM files and GDAL's sidecars, in case the plugin's output folder is ever
# pointed at this tree while testing. None of it belongs in the repository.
*.tif
*.tiff
*.hgt
*.hgt.gz
*.vrt
*.aux.xml
*.ovr
*.tfw

# QGIS scratch files
*.qgs~
*.qgz~
